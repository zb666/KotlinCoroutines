特性:
可重入，独享，悲观锁
锁的范围：类锁，对象锁，锁消除，锁粗化

锁被线程拿到之后，会被打上tag,每次进入和推出锁都会有标志

CAS自旋转一定次数之后，升级为重量级的锁。对象的markdown内部会保存一个监视器的地址

偏向锁(直接进行比较，减少在无竞争情况下JVM的资源消耗)
出现两个+的线程争取->
轻量级锁(CAS修改状态)->竞争一定次数没有抢到升级为
重量级锁

markdown中会包含四种Tag。
无锁，轻量锁，重量锁，gc废弃.

Lock如果一个线程拿到锁，其他线程会等待.
